model {for (i in 1:N){ 
                y[i] ~ dnorm(y.hat[i], tau)
                y.hat[i] <- a + b * x1[i] + c * x2[i] 
                }

            a ~ dnorm(-385.18,1/19.46) 
            b ~ dnorm(0.26,1/0.006)
            c ~ dnorm(-7.66,1/0.385)
            tau <- pow(sigma, -2) 
            sigma ~ dunif(0,30)  
            } 